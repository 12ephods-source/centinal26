name: dS regional Markov network invariant

on:
  push:
    branches: ["main", "physics/**"]
    paths:
      - "candidates/ds-regional-markov-network-invariant/**"
      - "candidates/ds-regional-modular-overlap/**"
      - "candidates/ds-diamond-relational-harness/**"
      - ".github/workflows/ds-regional-markov-network-invariant.yml"
  pull_request:
    paths:
      - "candidates/ds-regional-markov-network-invariant/**"
      - "candidates/ds-regional-modular-overlap/**"
      - "candidates/ds-diamond-relational-harness/**"
      - ".github/workflows/ds-regional-markov-network-invariant.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "${{ matrix.python-version }}"
      - run: python -m pip install --upgrade pip
      - run: >-
          python -m pip install
          -r candidates/ds-regional-markov-network-invariant/requirements.txt
          ruff==0.16.3
      - name: Lint candidate
        run: >-
          ruff check
          --output-format=github
          candidates/ds-regional-markov-network-invariant/network_invariant_scan.py
          candidates/ds-regional-markov-network-invariant/test_network_invariant_scan.py
      - name: Run strict network-invariant gates
        run: >-
          python candidates/ds-regional-markov-network-invariant/network_invariant_scan.py
          --strict
          --output ds-regional-markov-network-invariant-${{ matrix.python-version }}.json
      - name: Run regression tests
        run: >-
          python -m unittest discover
          -s candidates/ds-regional-markov-network-invariant
          -p 'test_*.py'
          -v
      - uses: actions/upload-artifact@v4
        with:
          name: ds-regional-markov-network-invariant-${{ matrix.python-version }}
          path: ds-regional-markov-network-invariant-${{ matrix.python-version }}.json
          if-no-files-found: error
