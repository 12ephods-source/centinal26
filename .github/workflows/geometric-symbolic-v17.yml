name: geometric-symbolic-v17

on:
  pull_request:
    paths:
      - 'experiments/geometric_symbolic_v11/**'
      - 'experiments/geometric_symbolic_v17/**'
      - '.github/workflows/geometric-symbolic-v17.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  matched-dose-replication-gate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - name: Verify exact PR head checkout
        if: github.event_name == 'pull_request'
        run: test "$(git rev-parse HEAD)" = "${{ github.event.pull_request.head.sha }}"
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
      - name: Install dependencies
        run: python -m pip install --upgrade pip numpy torch
      - name: Run frozen v17 experiment
        run: |
          set -o pipefail
          python experiments/geometric_symbolic_v17/geometric_symbolic_v17.py --steps 700 --out experiments/geometric_symbolic_v17/ci_results.json | tee experiments/geometric_symbolic_v17/ci_stdout.log
      - name: Apply frozen v17 checker
        run: |
          set -o pipefail
          python experiments/geometric_symbolic_v17/check_results.py experiments/geometric_symbolic_v17/ci_results.json | tee experiments/geometric_symbolic_v17/ci_verdict.json
      - name: Record environment
        if: always()
        run: |
          git rev-parse HEAD > experiments/geometric_symbolic_v17/ci_environment.txt 2>&1
          python --version >> experiments/geometric_symbolic_v17/ci_environment.txt 2>&1
          python - <<'PY' >> experiments/geometric_symbolic_v17/ci_environment.txt 2>&1
          import numpy, torch
          print('numpy', numpy.__version__)
          print('torch', torch.__version__)
          PY
          git --version >> experiments/geometric_symbolic_v17/ci_environment.txt 2>&1
      - name: Hash evidence
        if: always()
        run: |
          sha256sum experiments/geometric_symbolic_v17/geometric_symbolic_v17.py experiments/geometric_symbolic_v17/check_results.py experiments/geometric_symbolic_v17/ci_results.json experiments/geometric_symbolic_v17/ci_verdict.json experiments/geometric_symbolic_v17/ci_environment.txt > experiments/geometric_symbolic_v17/ci_sha256.txt
      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: geometric-symbolic-v17-evidence
          path: |
            experiments/geometric_symbolic_v17/ci_results.json
            experiments/geometric_symbolic_v17/ci_stdout.log
            experiments/geometric_symbolic_v17/ci_verdict.json
            experiments/geometric_symbolic_v17/ci_environment.txt
            experiments/geometric_symbolic_v17/ci_sha256.txt
