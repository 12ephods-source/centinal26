name: dS regional modular overlap

on:
  push:
    branches: ["main", "agent/**"]
    paths:
      - "candidates/ds-regional-modular-overlap/**"
      - "candidates/ds-diamond-relational-harness/**"
      - ".github/workflows/ds-regional-modular-overlap.yml"
  pull_request:
    paths:
      - "candidates/ds-regional-modular-overlap/**"
      - "candidates/ds-diamond-relational-harness/**"
      - ".github/workflows/ds-regional-modular-overlap.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "${{ matrix.python-version }}"
      - run: python -m pip install --upgrade pip
      - run: >-
          python -m pip install
          -r candidates/ds-regional-modular-overlap/requirements.txt
          ruff==0.16.3
      - name: Lint candidate
        run: >-
          ruff check
          --output-format=github
          candidates/ds-regional-modular-overlap/regional_modular_overlap_scan.py
          candidates/ds-regional-modular-overlap/test_regional_modular_overlap_scan.py
      - name: Run strict regional modular-overlap gates
        run: >-
          python candidates/ds-regional-modular-overlap/regional_modular_overlap_scan.py
          --strict
          --output ds-regional-modular-overlap-python-${{ matrix.python-version }}.json
      - name: Run regression tests
        run: >-
          python -m unittest discover
          -s candidates/ds-regional-modular-overlap
          -p 'test_*.py'
          -v
      - uses: actions/upload-artifact@v4
        with:
          name: ds-regional-modular-overlap-python-${{ matrix.python-version }}
          path: ds-regional-modular-overlap-python-${{ matrix.python-version }}.json
          if-no-files-found: error
