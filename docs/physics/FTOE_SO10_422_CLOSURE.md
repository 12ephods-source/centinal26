# FToE SO(10) → 422 → SM Closure State

Status: **REVIEW**

This branch replaces the previously assumed direct `SO(10) -> SM` unification path with a falsification-gated candidate chain

\[
SO(10) \xrightarrow{210_H} SU(4)_C\times SU(2)_L\times SU(2)_R \xrightarrow{126_H} G_{SM}.
\]

It does not promote the repaired branch to a prediction. Numerical execution, group-theoretic closure, threshold closure, and scientific closure remain separate gates.

## Corrected low-energy spectrum

The informational scalar is treated as a complex electroweak doublet

\[
I\sim(1,2,+1/2)
\]

with provisional physical threshold

\[
m_I=\sqrt{2}\,\mu_I\approx 13.5\ \mathrm{TeV}.
\]

For one additional complex Higgs-like doublet, the one-loop contribution is

\[
\Delta b=(1/10,1/6,0),
\]

not the previously used hypercharge contribution `5/18`.

Thus above `m_I`

\[
(b_1,b_2,b_3)=(21/5,-3,-7).
\]

The direct SM + I one-stage unification branch is therefore classified **FAIL**.

## Intermediate 422 gate

For the frozen candidate Pati-Salam coefficients

\[
(b_4,b_L,b_R)=(-7/3,2,28/3),
\]

and matching

\[
\alpha_4^{-1}=\alpha_3^{-1},\qquad
\alpha_{2L}^{-1}=\alpha_2^{-1},
\]

\[
\alpha_1^{-1}=\frac35\alpha_{2R}^{-1}+\frac25\alpha_4^{-1},
\]

one-loop elimination yields the intermediate-scale residual

\[
8\alpha_3^{-1}-21\alpha_2^{-1}+13\alpha_1^{-1}=0.
\]

`scripts/ftoe_so10_422_gate.py` solves this equation by residual-certified logarithmic bisection, then solves the 422 unification scale and checks all three inverse couplings independently.

## Informational hierarchy gate

Once `M_U` is derived, the candidate UV hierarchy is not hard-coded to a particular operator dimension. Instead the code scans

\[
\mu_I^2=c_n C_{\rm eff} M_U^2\left(\frac{M_U}{M_P}\right)^n
\]

and reports the integer `n` whose required `c_n C_eff` is closest to unity.

The downstream informational transition is then evaluated as

\[
-\mu_I^2+\frac{7.31\xi_I}{2}\frac{\Lambda_X^4}{M_P^2}=0,
\qquad \xi_I=1/6,
\]

followed by

\[
\beta=(\Lambda_X/M_P)^2.
\]

This tail is conditional until `mu_I` is independently generated by an explicit SO(10) operator and protecting symmetry.

## Fail-closed gates

The implementation intentionally remains **REVIEW** while any of these remain unresolved:

- FToE-specific two-loop running;
- full heavy threshold spectrum;
- explicit SO(10)-singlet mass-generating operator and Clebsch factor;
- proof that lower-dimensional symmetry-breaking operators are absent;
- proton-decay calculation from the same frozen heavy spectrum.

A software PASS must never be interpreted as a scientific PASS.

## Automation

GitHub Actions workflow `.github/workflows/ftoe-so10-422-gate.yml` runs the unit tests, emits a deterministic JSON closure record, hashes it with SHA-256, and uploads the artifacts for every relevant pull request or manual dispatch.

## Current decision

- Historical direct unification: **FAIL**.
- Intermediate non-D-parity 422 branch: **ACTIVE / REVIEW**.
- L1 informational-mass derivation: **REVIEW**.
- L3 threshold/unification closure: **REVIEW**.
- Proton-decay point prediction: **BLOCKED**.

No parameter may be changed after inspecting an observable it is intended to predict.
