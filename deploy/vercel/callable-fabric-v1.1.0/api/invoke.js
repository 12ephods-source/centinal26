'use strict';const{invoke}=require('../lib/fabric');module.exports=(req,res)=>{if(req.method!=='POST')return res.status(405).json({ok:false,error:{type:'METHOD_NOT_ALLOWED'}});const p=req.body||{};if(!p.service_id||!p.operation)return res.status(400).json({ok:false,error:{type:'BAD_REQUEST'}});const r=invoke(p.service_id,p.operation,p.arguments||{},p.context||{});return res.status(r.ok?200:(r.error&&r.error.type==='DENIED'?403:400)).json(r);};
