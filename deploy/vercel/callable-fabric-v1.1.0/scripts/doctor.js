'use strict';const assert=require('assert'),crypto=require('crypto');const{SERVICE_ID,CANONICAL_GIT_SHA,SOURCE_IDENTITY,SOURCE_IDENTITY_SHA256,ADAPTER_SPEC_SHA256,manifest,canonical,invoke,mcpTools,mcpCall,verifyReceipt}=require('../lib/fabric');assert.strictEqual(crypto.createHash('sha256').update(SOURCE_IDENTITY).digest('hex'),SOURCE_IDENTITY_SHA256);assert.strictEqual(CANONICAL_GIT_SHA,'6e5731bfbb073573c044bcc2e106d3906f6dddf5');assert.strictEqual(manifest.policy.unrestricted_remote_shell,false);const h=invoke(SERVICE_ID,'system.health',{},{});assert(h.ok&&verifyReceipt(h.receipt));const e=invoke(SERVICE_ID,'frost.diagnostics.echo',{text:'Frost Ω'},{});assert.strictEqual(e.result.length,7);assert(verifyReceipt(e.receipt));const s=invoke(SERVICE_ID,'frost.diagnostics.sha256',{text:'abc'},{});assert.strictEqual(s.result.sha256,'ba7816bf8f01cfea414140de5dae2223b00361a396177a9cb410ff61f20015ad');const d=invoke(SERVICE_ID,'frost.diagnostics.dangerous_demo_policy_test',{target:'negative-control'},{role:'operator',approved:false});assert.strictEqual(d.error.type,'DENIED');assert(verifyReceipt(d.receipt));const bad=invoke(SERVICE_ID,'frost.diagnostics.echo',{text:42},{});assert.strictEqual(bad.ok,false);assert.strictEqual(mcpTools().length,6);assert(mcpCall('frost_diagnostics_echo',{text:'mcp'},{}).ok);const spec={service:'frost-callable-fabric',version:'1.1.0',canonical_repo:'12ephods-source/centinal26',canonical_git_sha:CANONICAL_GIT_SHA,source_identity_sha256:SOURCE_IDENTITY_SHA256,protocols:['frost-call/1.0','mcp/2025-06-18'],operations:['system.health','system.capabilities','frost.diagnostics.echo','frost.diagnostics.sha256','frost.diagnostics.canonicalize','frost.diagnostics.dangerous_demo_policy_test'],policy:{semantic_tools_only:true,unrestricted_remote_shell:false,guardian:true,schema_validation:true,provenance_receipt:true}};assert.strictEqual(crypto.createHash('sha256').update(canonical(spec)).digest('hex'),ADAPTER_SPEC_SHA256);console.log(JSON.stringify({ok:true,tests:10,canonical_git_sha:CANONICAL_GIT_SHA,source_identity_sha256:SOURCE_IDENTITY_SHA256,deployment_adapter_sha256:ADAPTER_SPEC_SHA256}));
